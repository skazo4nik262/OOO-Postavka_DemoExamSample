<Window x:Class="OOO_Postavka.View.AddEditUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OOO_Postavka.View"
        mc:Ignorable="d"
        Title="Пользователь" Height="350" Width="350" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" WindowStyle="ToolWindow">
    <StackPanel Margin="20" VerticalAlignment="Center">
        <Label Content="Логин:"/>
        <TextBox Text="{Binding Login}"/>
        <Label Content="Пароль:"/>
        <TextBox Text="{Binding Password}"/>
        <Label Content="Подтвердите пароль:"/>
        <TextBox Text="{Binding PasswordControl}"/>

        <CheckBox IsChecked="{Binding IsBlocked}" Content="Заблокирован" Visibility="{Binding VisibilityContent}"/>
        <ComboBox SelectedItem="{Binding Role}" ItemsSource="{Binding Roles}" DisplayMemberPath="Title"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Сохранить" Click="SaveClick" Width="80" Margin="5"/>
            <Button Content="Отмена" Click="CancelClick" Width="80" Margin="5"/>
        </StackPanel>
    </StackPanel>
</Window>
